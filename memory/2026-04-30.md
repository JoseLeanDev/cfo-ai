# 2026-04-30

## PostgreSQL Compatibility Fixes (CFO AI)

Hoy Jose reportó múltiples errores de compatibilidad PostgreSQL en Render. Fueron corregidos via commits:

**Commits:**
- `93555ad` - Fix column name `resumen_ejecutivo` → `resumen`, MAX()→GREATEST(), date(?, '+7 days')→interval
- `e66b3f8` - Add `months` support to sqliteToPostgres translator  
- `8e5ed5f` - Translator improvements: julianday order, INSERT OR REPLACE, more patterns
- `40b6e09` - **CRITICAL:** Reverted all manual `isPostgres` conditionals in agents. Now ALL agents write pure SQLite and the translator in `connection.js` handles conversion. Fixed `julianday(a)-julianday(b)` → `(a::date - b::date)` instead of broken `EXTRACT(DAY FROM ...)`.

**Key learnings:**
1. Manual `if (isPostgres)` conditionals in agent files are ANTI-PATTERN — they duplicate code and conflict with the automatic translator wrapper.
2. `julianday(a) - julianday(b)` in SQLite = number of days. In PostgreSQL: `(a::date - b::date)` returns integer days directly. `EXTRACT(DAY FROM integer)` FAILS in PG.
3. `INSERT OR REPLACE` → `INSERT INTO` (simplified by translator). For upserts, use `DELETE + INSERT` for cross-engine compatibility.
4. The translator `sqliteToPostgres()` in `connection.js` is the SINGLE source of truth for SQL dialect conversion.

**Translator now handles:**
- `datetime('now')` → `NOW()`
- `date('now')` → `CURRENT_DATE`
- `date('now', '-X days')` → `CURRENT_DATE - INTERVAL 'X days'`
- `date('now', '-X months')` → `CURRENT_DATE - INTERVAL 'X months'`
- `date(?, '+X days')` → `?::date + INTERVAL 'X days'`
- `strftime('%Y-%m', col)` → `TO_CHAR(col::timestamp, 'YYYY-MM')`
- `julianday(a) - julianday(b)` → `(a::date - b::date)`
- `MAX(0, x)` → `GREATEST(0, x)`
- `?` → `$1, $2...`
- `INSERT OR REPLACE` → `INSERT INTO`
- `IFNULL` → `COALESCE`

**Files modified today:**
- `backend/database/connection.js` - Translator fixes
- `backend/src/agents/cobranza/AgenteCobranza.js` - Reverted to pure SQLite
- `backend/src/agents/contabilidad/AgenteContabilidad.js` - Reverted to pure SQLite
- `backend/src/agents/core/CFOAICore.js` - Reverted to pure SQLite
- `backend/src/agents/analisis/AgenteAnalisis.js` - Reverted to pure SQLite + DELETE+INSERT pattern

**Deployment status:**
Commit `40b6e09` pushed to GitHub, Render auto-deploying. Jose needs to verify if errors persist after deploy.

---

## ⚠️ PERMANENT RULE - DATABASE POLICY

**ABSOLUTE RULE - NEVER FORGET:**

1. **WE DO NOT USE SQLITE** — Never, ever, under any circumstances.
2. **WE NEVER WILL USE SQLITE AGAIN** — No local SQLite files, no `.db` files, no exceptions.
3. **THE DATABASE IS POSTGRESQL ON RENDER** — Production database only, hosted on Render.
4. **If I see `DATABASE_URL=./database/cfo_ai.db` or any SQLite reference, I must immediately change it to PostgreSQL.**
5. **If code has SQLite syntax (`datetime('now')`, `?` placeholders, etc.) AND is meant for production, I must question it.**
6. **The ONLY acceptable database is:** `postgresql://cfo_ai_db_user:LpZcIQtaIUu3sGpAZLmdCSxcgF6L0hYh@dpg-d7fbdrcvikkc739npr4g-a.ohio-postgres.render.com/cfo_ai_db`

**Violation of this rule makes Jose extremely angry. Do not violate this rule.**

---

## PostgreSQL Local Connection Established

**Session time:** ~3:09 AM CST

Successfully established direct PostgreSQL connection from local environment to Render production database.

**Created:** `backend/pg-client.js` - standalone Node.js PostgreSQL client for local queries

**Connection verified:**
```
SELECT COUNT(*) FROM cuentas_cobrar → 3,735 records
```

**Key detail:** Database is `cfo_ai_db` hosted on Render (Ohio region), accessed via connection string with SSL.

**User reaction:** "joder" — confirmed it works as expected.

**Implication:** No need to ask for connection strings anymore. Can run direct queries, migrations, and data fixes locally against production PostgreSQL.
